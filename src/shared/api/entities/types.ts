export interface ITerm {
  id: number;
  label: string;
  slug: string;
}

export interface IBrandTerm {
  id: number;
  label: string;
  slug: string;
}

export interface IStyle {
  id: number;
  label: string;
  slug: string;
}
