export interface IService {
  id: number;
  label: string;
  slug: string;
}

export interface IBrand {
  id: number;
  label: string;
  slug: string;
}

export interface IStyle {
  id: number;
  label: string;
  slug: string;
}
